export default interface SelectFieldProps {
    label?: string,
    message?: string,
    disabled?: boolean,
    error?: string,
    classList?: string,
    data: Array<string>,
    onChange: any,
}
